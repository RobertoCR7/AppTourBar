

<p id="notice"><%= notice %></p>

<div class="panel-group">
  <div class="row">
    <div class="col-sm-3"></div>
    <div class="col-sm-6">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-2">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
            <a class="navbar-brand" href="<%= informacion_path %>">Info</a>
          </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-2">
            <ul class="nav navbar-nav">
            <li class="nav-item <%= 'active' if current_page?(vestimenta_path) %>"><a href="<%= vestimenta_path %>">Vestimenta</a></li>
              <li class="nav-item <%= 'active' if current_page?(promociones_path) %>"><a href="<%= promociones_path %>">Promociones</a></li>
              <li class="nav-item <%= 'active' if current_page?(eventos_path) %>"><a href="<%= eventos_path %>">Eventos</a></li>
              <li class="nav-item <%= 'active' if current_page?(compras_path) %>"><a href="<%= compras_path %>">Compras</a></li>
              <li class="nav-item <%= 'active' if current_page?(ofreces_path) %>"><a href="<%= ofreces_path %>">Te Ofrece</a></li>
              <li class="nav-item <%= 'active' if current_page?(reservacions_path) %>"><a href="<%= reservacions_path %>">Reservaciones</a></li>

            </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>
    </div>
    <div class="col-sm-3"></div>
  </div>
  <div class="row">
    <div class="col-sm-2"></div>
    <div class="col-sm-8">
      <div class="panel panel-default">
        <div class="panel-body"><h1 align="center" style="color: #fff;">

        Calendarios de Eventos

        </h1> </div>
      </div>
    </div>
    <div class="col-sm-2">
    </div>
  </div>
  
  <div  class="col-sm-10">


      <% @eventos.each do |evento| %>
      
<p>
      <strong>Fecha</strong>
        <%= evento.dia.strftime("%d-%m-%y") %>

      


 <p>        
        <%= image_tag(evento.foto, :style => 'width:200px') %>
</p>

<p>
        <strong>Descripcion</strong>
        <%= evento.descripcion %>
</p>
         
        <td>
            <div id="class"class="btn-group-orizontal">
            <button type="button" class="btn btn-default"><%= link_to 'Show', evento %></button>
            <button type="button" class="btn btn-primary"><%= link_to 'Edit', edit_evento_path(evento) %></button>
            <button type="button" class="btn btn-danger"><%= link_to 'Destroy', evento, method: :delete, data: { confirm: 'Are you sure?' } %></button>

          </div></td>

      </tr>
        <% end %>
    

  <br>
  <button id="new" type="button" class="btn btn-primary"><%= link_to 'Nuevo Evento', new_evento_path %></button>
  <a class="btn btn-default" href="<%= even2_path %>">Video de CNCO</a>
  <a class="btn btn-default" href="<%= even_path %>">Video de LOS tigres del norte</a>
  <a class="btn btn-default" href="<%= even3_path %>">Video de Cafe Tacvba</a>
  </br>
</div>

