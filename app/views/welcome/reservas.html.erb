

<p id="notice"><%= notice %></p>

<div class="panel-group">
  <div class="row">
    <div class="col-sm-3"></div>
    <div class="col-sm-6">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-2">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
            <a class="navbar-brand" href="<%= informacion_path %>">Info</a>
          </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-2">
            <ul class="nav navbar-nav">
            <li class="nav-item <%= 'active' if current_page?(uservestimenta_path) %>"><a href="<%= uservestimenta_path %>">Vestimenta</a></li>
              <li class="nav-item <%= 'active' if current_page?(userpromociones_path) %>"><a href="<%= userpromociones_path %>">Promociones</a></li>
              <li class="nav-item <%= 'active' if current_page?(usereventos_path) %>"><a href="<%= usereventos_path %>">Eventos</a></li>
              <li class="nav-item <%= 'active' if current_page?(compras_path) %>"><a href="<%= compras_path %>">Compras</a></li>
              <li class="nav-item <%= 'active' if current_page?(userofreces_path) %>"><a href="<%= userofreces_path %>">Te Ofrece</a></li>
              <li class="nav-item <%= 'active' if current_page?(userreservacions_path) %>"><a href="<%= userreservacions_path %>">Reservaciones</a></li>

            </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>
    </div>
    <div class="col-sm-3"></div>
  </div>
  <div class="row">
    <div class="col-sm-2"></div>
    <div class="col-sm-8">
      <div class="panel panel-default">
        <div class="panel-body"><h1 align="center" style="color: #fff;">

        Reservaciones

        </h1> </div>
      </div>
    </div>
    <div class="col-sm-2">
    </div>
  </div>
  <div class="row">
    <div class="col-sm-2"></div>
 <div class="col-sm-9.5" >
      <div class="panel panel-default">
        <div class="panel-body" ><h4 align="izquierda">


<div class="container">
  <table id="table" class="table table-striped">
    <thead>
      <tr>
        <th><strong>Mesa</strong></th>
        <th><center>Nombre reservador</center></th>
        <th>Disponible</th>
        <th>Accion</th>
      </tr>
    </thead>

    <tbody BGCOLOR="#B9B9B9" TEXT="red">
      <% @reservas.each do |reserva| %>
      <tr>
        <td><center><%= image_tag(reserva.mesa, :style => 'width:200px') %></center></td>
        <td style="color: black"><%= reserva.nombre %></td>
        <td style="color: black"><%= reserva.reserva %></td>
        <td>
            <% if reserva.reserva == false %>
            <button type="button" class="btn btn-primary"><%= link_to 'Reservar Mesa', edit_reserva_path(reserva) %></button>
            <% else %>
            <button type="button" class="btn btn-primary">no Disponible</button>
              <% end %>
          </div></td>
      </tr>
        <% end %>
    </tbody>
  </table>
  <br>



  </br>
</div>
</center>
