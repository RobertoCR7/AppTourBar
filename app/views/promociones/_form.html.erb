<%= form_with(model: promocione, local: true) do |form| %>
  <% if promocione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promocione.errors.count, "error") %> prohibited this promocione from being saved:</h2>

      <ul>
      <% promocione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_area :titulo, id: :promocione_titulo, :class => 'form-control'  %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion, id: :promocione_descripcion, :class => 'form-control'  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
