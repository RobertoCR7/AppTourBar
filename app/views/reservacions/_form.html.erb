<%= form_with(model: reservacion, local: true) do |form| %>
  <% if reservacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reservacion.errors.count, "error") %> prohibited this reservacion from being saved:</h2>

      <ul>
      <% reservacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.text_area :cantidad, id: :reservacion_cantidad %>
  </div>
  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_area :nombre, id: :reservacion_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
